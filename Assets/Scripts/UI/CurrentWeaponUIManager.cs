using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;


public class CurrentWeaponUIManager : MonoBehaviour
{
    public PlayerStatsManager psm;



    WeaponManager wm;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //wm = psm.currentPrimary.GetComponent<WeaponManager>();


    }
}
